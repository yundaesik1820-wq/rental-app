# 강제 업데이트 게이트 — 내가 해야 할 것 (체크리스트)

작성일 2026-07-12. push 하기 전/후로 이 순서대로 하면 됨.

---

## ✅ 이미 코드에 반영됨 (내가 안 건드려도 되는 것)
- `src/appVersion.js` — 버전 상수 + 비교 함수 (신규)
- `src/components/UpdateGate.jsx` — 못 닫는 업데이트 모달, 네이티브 앱에서만 동작 (신규)
- `src/App.jsx` — `<AuthProvider>`를 `<UpdateGate>`로 감쌈
- `CLAUDE.md` — 1.0.6(=1.6) 승인 기록
- 빌드 검증 완료 (`npm run build` 통과)

---

## 📋 STEP 1. Firestore 콘솔에 설정 문서 만들기
Firebase 콘솔 → Firestore Database

- **컬렉션**: `config`
- **문서 ID**: `appVersion`
- **필드**:
  | 필드 | 타입 | 값 |
  |------|------|-----|
  | `minVersion` | string | `1.0.7` |
  | `enabled` | boolean | `true` |
  | `message` | string | (선택) 비우면 기본 문구 |
  | `iosUrl` | string | (선택) 비우면 기본 앱스토어 링크 |
  | `androidUrl` | string | (선택) 비우면 기본 플레이스토어 링크 |

> 나중에 특정 버전 이하를 막고 싶을 땐 **`minVersion` 값만 올리면 끝** (앱 재배포 X, 실시간 반영).

---

## 📋 STEP 2. Firestore 보안 규칙 추가
Firebase 콘솔 → Firestore → 규칙(Rules)에 아래 블록 추가 후 **게시**:

```
match /config/{doc} {
  allow read: if true;
  allow write: if false;
}
```
- 누구나 읽기 가능(로그인 전에도 버전 확인 필요), 쓰기는 콘솔에서만.

---

## 📋 STEP 3. 다음 릴리스 때마다 버전 3곳 맞추기 (중요)
새 버전 배포할 때 **아래 3개 숫자를 항상 똑같이** 올린다:

1. `src/appVersion.js` → `APP_VERSION = "1.0.7"`
2. iOS: `ios/App/App.xcodeproj/project.pbxproj` → `MARKETING_VERSION` (+ 빌드번호 `CURRENT_PROJECT_VERSION`)
3. Android: `android/app/build.gradle` → `versionName` (+ `versionCode`)

> `appVersion.js`를 안 올리면 게이트가 버전을 잘못 판단함. 릴리스 = 이 3개 세트로 기억.

---

## 📋 STEP 4. push & 배포
```
GitHub Desktop으로 push → Vercel 자동 배포 (웹)
iOS: 빌드번호 ↑ → Codemagic → TestFlight → App Store Connect
Android: versionCode ↑ → AAB 빌드 → Play Console
```

---

## ⚠️ 반드시 기억할 함정
- 이 게이트 코드는 **지금 앱 번들엔 없음.** 이 코드가 들어간 **다음 릴리스(1.0.7)부터만 작동**.
- 그래서 **지금 깔려있는 1.0.6 이하 사용자는 강제 못 함** (그들 앱엔 검사 코드가 없음).
- 1.0.7을 배포한 뒤부터, 그 이후 버전들에 대해서만 강제가 먹힘. → "지금부터 앞으로"용 장치.

---

## 🔧 나중에 실제로 강제하고 싶어질 때 (예: 1.0.8 필수)
1. `config/appVersion.minVersion` 을 `1.0.8` 로 변경
2. 끝. 1.0.7 사용자 앱에 즉시 업데이트 모달 뜸.
