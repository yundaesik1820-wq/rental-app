# 작업 내역 — 2026-07-18 (홈 화면 · 시간표 개편)

## 1. 상단바 전면 통일
- 공용 헤더(`Layout.jsx`)를 커뮤니티 헤더 기준으로 교체 — 띠(border) 제거, 큰 볼드 제목, 검색·알림 아이콘.
- 커뮤니티 헤더 포함 **제목·아이콘 모두 20px**로 통일 + 수직 정렬.

## 2. 홈 히어로 카드
- CSS 재현 시도 → 최종적으로 **`home-hero.png` 이미지 그대로 사용** (투명 여백 크롭해서 펫 박스와 폭 일치).
- 좌측 상단 **인사 텍스트 오버레이** ("안녕하세요, {이름}님 👋" / "오늘도 멋진 촬영과 작품을 / 한예진이 함께 응원할게요!") — 자간 0.02em.
- **로그아웃 + 계정전환(연동계정 있는 사람만)** 아이콘만, 장비예약 카드 왼쪽선 맞춤·세로 중앙. 로그아웃 먼저.
- 이미지 위 **버튼 클릭영역 3개**(장비예약→장비탭 / 예약내역→예약내역탭 / 대여규칙→규칙모달) + 스무스 누름 모션.

## 3. 레벨/신뢰도 카드
- 펫 카드에 **실제 대여 통계**(대여·정시반납·연체) + **신뢰도 점수**(정시반납×120 + 대여×30 − 연체×300).
- ⚠️ 정시반납은 현재 "반납완료" 전부를 정시로 카운트 → 연체이력 구분은 TODO.

## 4. 시간표 섹션 개편 (tlrksvy.png)
- **주간 그리드**: 토요일 제거(월~금), 흰색→다크 네이비 톤.
- **다음 수업 카드**: 현재시각 기준 자동 계산("N분 후 시작"/"진행 중"), 1분마다 갱신.
- **오늘의 수업 리스트**: 오늘 요일만, 2줄(과목명 / 시간·장소·교수), 행 통일.
- **친구 시간표**: 그리드와 같은 네이비 톤으로 통일 (다음수업/오늘 리스트는 미포함).
- **시간표 없을 때(empty state)**: ui.png 디자인 — 캘린더 일러스트 + 안내 2줄 + [직접 추가]/[AI로 추가] 버튼.
- **직접추가 폼 / AI로추가 모달**: 홈 톤(퍼플·블루 그라데이션)으로 정리, 버튼 크기 통일.

## 5. 버그 근본 수정
- **전역 그라데이션 padding 함정**: `Layout.jsx`의 `[style*="linear-gradient"]` 광역 규칙이 앱 전체 인라인 그라데이션 요소에 `padding:18px` 강제 → 그라데이션 버튼만 커지는 등 왜곡. **`[data-hbanner]` 배너 전용으로 좁힘**, 설치배너 2개에 마커.

## 확인/후속
- [ ] 실기기: 다음수업/오늘 리스트(평일+데이터 있을 때), 히어로 버튼 클릭·정렬, 펫/그라데이션 요소 정상 여부.
- [ ] 신뢰도 정시반납 정확화(반납 시점 연체 플래그) — 나중에.
- [ ] 학점계산기 패널 톤(현재 흑백) — 친구시간표만 네이비로 바꿔 둘이 다름. 원하면 통일.
- 배포: 모두 push 완료 (Vercel 자동 배포).
