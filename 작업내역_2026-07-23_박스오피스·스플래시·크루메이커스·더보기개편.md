# 작업 내역 — 2026-07-23 박스오피스 리디자인·룸 스플래시·크루 메이커스·더보기 개편

대상 파일: `src/pages/student/Community.jsx`, `src/App.jsx`, `src/components/Layout.jsx`, `src/hooks/useAuth.jsx`, `src/pages/student/Profile.jsx`·`FriendManager.jsx`·`Inquiry.jsx`·`License.jsx`, `src/pages/admin/Notices.jsx`, `public/boxoffice-splash.webp`·`class-splash.webp`(신규)
커밋 범위: `96c9ada`(작업 전) → `0b9de33`(현재), 총 9개.
배포: GitHub push → Vercel 자동배포.

---

## 1. KBATV 박스오피스 블루 리디자인 (`7985403`)
- 목업(dd.png) 기준 넷플릭스 레드 → **블루·퍼플 톤** 전환. `BoxOfficeView`·`WorkCard`·`CarouselRow`만 수정, 데이터 모델 무관.
- 히어로: 이미지가 배경으로 **그라데이션 페이드**, 퍼플 FEATURED, `oneLiner` 한 줄 소개, **"▶ 재생하기" 블루→퍼플 그라데이션 필 버튼** + 원형 ⓘ, 도트 인디케이터 히어로 아래 중앙(활성=블루 `#3b82f6`).
- 섹션 헤더: 이모지 제거, "인기작 **TOP 10**" 블루 텍스트, 우측 "전체 보기 ›".
- 카드: 둥근 모서리 + 얇은 블루 테두리/글로우. 최신작 **7일 이내 NEW 빨강 배지**(`boIsNew`). TOP 10은 좌하단 **대형 랭킹 숫자**.
- **전체 보기** → 해당 섹션 작품 **2열 그리드** 뷰(`allView` state, ‹ 뒤로가면 캐러셀 복귀).
- 배경: 다크 네이비 그라데이션(`#0c1124→#0a0a12`).

## 2. 룸 진입 스플래시 (`298c0b0`, `a357364`)
- 커뮤니티 홈 **오늘의 추천작 전체보기** → 스플래시 이미지가 화면 전체를 덮고 밑에서 박스오피스 진입 → **0.9초 유지 → 0.6초 페이드아웃**.
- 사용자 제작 1080×2400 PNG(2.7MB)를 **sharp-cli WebP q80으로 압축**: 박스오피스 100KB, 클래스 120KB.
- `createPortal(document.body)` + zIndex 100000 — 커뮤니티 z-200 컨테이너·하단바(250)·영상뷰어(99999) 전부 위. `maxWidth:"none"` 인라인(전역 max-width 함정 회피).
- `a357364`에서 **`runRoomSplash(src)` 공용화** — 필름 클래스 전체보기에도 적용(`class-splash.webp`). 커뮤니티 진입 시 두 이미지 프리로드. 다른 룸 추가는 이미지 + 한 줄이면 됨.

## 3. 크루 메이커스 홈 섹션 + 북마크 (`6a1caa0`, `77b35b3`)
- 에타·질문 최신글 그리드와 정보 최신글 사이 **크루 메이커스** 섹션(dsfds.png 목업). 전체보기 → crew 룸(협업모집 탭).
- `77b35b3`에서 **좌우 반반 2열**로 전환(`CrewMakersSection` 컴포넌트 추출): **좌 = 구인글**(협업모집, 마감 안 지난 최신 6개), **우 = 스탭 프로필**(스탭프로필 최신 6개). 각 열 **독립 자동 순환**(5초) + 도트 탭 이동, 카드 높이 196px 고정(`boxSizing:"border-box"` — content-box로 우측만 223px 되던 함정 잡음).
- 구인글 카드: 썸네일/모집중·**D-3 이하 마감임박** 배지/포지션 칩 2개/📅📍/감독명·`👥 지원/정원`(가득 차면 노랑). 스탭 카드: 원형 사진(없으면 🙋)/실명/전공/핑크 담당분야 칩.
- **⭐ 북마크 신설**: 별 탭 → `users/{uid}.crewBookmarks` 배열 `arrayUnion/arrayRemove`(seenNotifs 패턴, **규칙 수정 불필요**). 낙관적 로컬 갱신. ⚠️ **모아보기 화면은 아직 없음** — 후속 후보.
- Project Studio 팀원 모집글은 **같은 스키마**(adapters.js `createRecruitmentPost`)라 동일하게 노출. 단 `images:[]` 고정(항상 🎬 폴백), 포지션 항상 1개/1명, `fromProjectId`로 출처 구분.

## 4. 더보기 헤더 개편 (`3e5ddb8`, `36bb711`)
- 더보기 탭 헤더 "내 정보" → **"더보기"**(STU_NAV 라벨). 하위 메뉴 진입 시 헤더에 **‹ 뒤로가기 + 메뉴 이름** 표시.
- 구현: `StudentMyPage`의 `view` state를 **App으로 리프팅**(`mypageView`) → Layout에 **`headerTitle`/`onHeaderBack` prop 신설**. 본문 "← 더보기" 버튼 제거. 재탭 메뉴 복귀·탭 이탈 초기화·알림 딥링크 유지. CLAUDE.md 갱신.
- `36bb711` **PageTitle 중복 정리**: 헤더 제목과 겹치는 5개 화면(Profile·FriendManager·student Inquiry·student License·admin Notices) 타이틀 제거 + 미사용 import 정리. 문의하기/공지 작성 버튼은 우측 정렬 유지, 학생 공지는 빈 행 자체 제거.

## 5. 계정 전환 세션 어긋남 복구 (`c8fd81f`)
- **버그**: 학생으로 쓰다 앱을 죽였는데 재실행하면 관리자로 열림. 원인 = 계정 전환이 `signInWithEmailAndPassword`로 갈아타는 방식인데 **네이티브 WebView가 Firebase IndexedDB 세션 쓰기를 늦게 flush** → kill 시 최근 로그인 유실, 이전 세션 부활.
- 수정(`useAuth.jsx`): 로그인 시 `active_email`을 **localStorage(동기)에 기록**. 앱 시작 **첫 auth 콜백에서만** 복원 세션 ≠ active_email이면 `linked_creds_{uid}`(기존 전환용 저장 자격증명)로 **1회 자동 재로그인**. 재로그인 중 스피너 유지(로그인 화면 깜빡임 방지), 실패 시 복원 세션 그대로, 명시적 로그아웃 시 기록 삭제.
- 테스트: 관리자→전환→학생→즉시 앱 스와이프 종료→재실행→학생이면 성공 (실기기).

## 6. 더보기 전면 리디자인 + 설정 화면 신설 (`0b9de33`)
- 목업(ejqhrl.png) 기준 `StudentMyPage` 전면 교체 (블루·퍼플).
- **프로필 히어로 카드**: 퍼플 그라데이션 + 글로우 링 아바타(사진 필드 없어 이니셜 `Avatar`) + ✏️(→내 정보) + 학번·계열 + **KBAS MEMBER** 배지 + 🎬 장식.
- 메뉴 5개: 이모지 → **lucide 아이콘 타일**(톤별 색). `Settings`는 관리자 페이지와 이름 충돌 → `Settings as SettingsIcon`.
- **라이선스 카드**: 퍼플 하이라이트 + CREW LEVEL 필 + 그라데이션 프로그레스 바 + `Lv. n`(`profile.license` LV0~3 파싱, n/3 비율).
- 하단 그룹: **설정 + 로그아웃**(빨강, `window.confirm` 후 `logout()`) 한 카드 두 행.
- **`StudentSettings` 신설**(더보기 › 설정): 푸시 알림 권한 상태 표시 + 미설정 시 **알림 켜기**(기존 `enableNotifications` 재사용) + 거부 시 기기 설정 안내 / 앱 정보(버전 `APP_VERSION`·플랫폼). `MYPAGE_TITLES`에 settings 추가로 헤더 ‹/제목 자동 적용.

---

## 남은 확인
- 박스오피스·스플래시·크루 2열·더보기: 실기기(갤럭시23/아이폰16) 확인.
- 계정 전환 복구: 전환 직후 앱 kill → 재실행 시나리오 실기기 테스트.
- 스탭프로필 글 0건이면 크루 오른쪽 열은 점선 플레이스홀더 — 테스트 글 올려 확인.
- 크루 북마크 **모아보기 화면 미구현** (별 저장만 됨) — 후속 작업 후보.
- Project Studio발 모집글 썸네일 없음(항상 🎬) — 프로젝트 썸네일 연동 개선 여지.
