# 작업 내역 — 2026-07-22 커뮤니티 대개편 + 안정성

대상 파일: 대부분 `src/pages/student/Community.jsx` (일부 `src/App.jsx`, `public/boxoffice-banner.png` 신규)
커밋 범위: `26c0543`(작업 전) → `747d7ef`(현재), 총 17개.
배포: GitHub push → Vercel 자동배포. 빌드 매 단계 `npm run build` 통과.

---

## 1. 검색바 개편 (`f169199`)
- **익명게시판 부제 제거** (커뮤니티 룸에서 "🔒 익명 게시판" 삭제, 빈 여백도 안 남게 조건부 렌더).
- 검색창 좌측에 **제목/내용 드롭다운** 추가 → 선택한 범위로만 필터 (`searchScope` state). 제목 스코프엔 강의명·교수명도 포함.
- placeholder "제목, 내용 검색" 예시 텍스트 제거.
- 우측 검색 아이콘을 이모지 🔍 → **lucide `Search` SVG 단색**으로 (모바일 웹앱에서 컬러 돋보기로 뜨던 문제 해결).

## 2. TOP3 → 박스오피스 배너 이미지 (`fabaeaa`, `5a026ec`)
- `top image.png` → `public/boxoffice-banner.png`로 저장, TOP3 카드를 **이미지 전체 배경**으로 바꾸고 빈 필름릴 무대 위에 인기글 3줄을 얹음(텍스트 그림자로 가독성).
- 배너 좌우 폭을 아래 게시글 박스에 정렬: PNG 바깥 죽은 글로우를 트림(`1891×746`), `aspect-ratio` + `background-size:100% 100%`.
- 검색 드롭다운 화살표를 네이티브 → **커스텀 chevron**으로(텍스트는 그대로, 화살표만 구분선에서 띄움).

## 3. 게시글 상세 페이지 리디자인 (`0e9460c`, `9edc1ae`, `10c4b14`) — `word.png` 목업
- 카테고리 뱃지, 제목, 메타(`조회 N`), **리액션 알약**, `댓글 N` 헤더, **테두리 댓글 카드**, 레드 등록 버튼 등 전면 리디자인.
- 신고/차단 → 글 박스 **우측 상단 점세개(⋮) 메뉴**(눌러야 열림, 바깥 클릭 시 닫힘). 이모지 제거.
- 리액션 순서: 하트-비추-댓글 → **따봉(👍)-댓글(💬)-비추(👎)**. 좋아요가 하트→따봉으로.
- 따봉/비추 이모지 **비활성 시 무색(grayscale), 눌러야 컬러** (게시글·댓글 공통).
- 댓글 시간 폰트를 게시글 시간과 동일(monospace 제거), 댓글 리액션 하트→따봉.
- 댓글 입력창 하단 "익명으로 게시됩니다" 문구 제거.
- 기존 기능(추천·비추·신고·차단·관리자삭제·실명댓글·강의 별점) 전부 보존.

## 4. 톤 통일 — 앱 파랑→보라 그라데이션 (`80f9e33` → `901e38d`, `a1ff393`, `9b864a3`)
- 처음엔 자유=빨강 / 질문=파랑 분기(`80f9e33`)였다가, **앱 톤(`linear-gradient(135deg,#5b8def,#7c3aed)`)으로 전면 통일**로 변경.
- 적용 범위: 게시글 상세(뱃지·알약 아우라·댓글수·따봉 활성색·입력창 테두리·등록 버튼), 새 글 작성 폼(카테고리 버튼·게시하기 버튼), **커뮤니티 카테고리 탭**, **글쓰기 FAB**.
- `postAccent`를 카테고리 무관 통일 액센트 객체(`gradient/solid/bright/border/glow`)로 정리.

## 5. 새 글 작성 폼 (`901e38d`) — `print.png` 목업
- `● REC` 제거, 제목 확대, `CATEGORY`(monospace) → `카테고리`(sans) 폰트.
- 카테고리 안내문구 제거, 하단 경고 **박스 제거하고 평문 처리**.
- 카테고리/게시 버튼 그라데이션.

## 6. 정보공유 탭 개편 (`e4c9e6d`, `b61ba1c`, `d60b2c8`, `747d7ef`)
- 정보공유 룸 카테고리: `강의·정보·취업·공모전` → **정보 / 공모전 / 기타**.
- 정보/공모전/기타를 `REAL_CATS`에서 빼고 **모두 익명 처리**(`ANON_CATS`). `기타` 카테고리 색 추가.
- 내부 UI·작성폼·상세·목록은 기존 generic이라 커뮤니티와 동일하게 동작(탭·FAB 그라데이션 이미 통일됨).
- **홈에 "정보 최신글" 섹션 신설** (`acac.png` 스타일): 썸네일(첫 이미지, 없으면 📄) + 제목 + `익명` + `👁 조회 · 💬 댓글 · 시간`. "전체보기" → 정보공유 탭.
  - 정보+공모전+기타 통합 최신 3개, 제목 12px, 시간 **상대표기**(`timeAgo`: 방금/분/시간/일 전).
  - 행 여백 축소(패딩 6px, 썸네일 46px).
- 정보공유 룸 부제 문구 제거.
- ⚠️ 기존 `강의`·`취업` 글은 삭제 안 함 → 정보공유 탭에서만 안 보임(Firestore엔 잔존). 필요 시 마이그레이션 별도.

## 7. 기타 정리
- 게시글 목록 페이지네이션 하단 `1~N / 전체 n개` 안내 제거 (`9292625`).
- 글쓰기 FAB이 하단바(zIndex 250)에 가려지던 문제 → `bottom: navH + 16`으로 올림 (`4fc6936`).

## 8. ⭐ 안정성 — 검은 화면 먹통 방지 (`0057ddf`) — `src/App.jsx`
- **원인**: 거의 모든 페이지가 `React.lazy`인데 ErrorBoundary가 없어, 청크 로드 실패(Vercel 재배포로 옛 해시 소실·WebView 네트워크 순간끊김) 시 트리 전체 언마운트 → 검은 화면.
- **`lazyRetry`**: 청크 import 2회 재시도, 소진 시 `sessionStorage` 가드 하에 새로고침 1회로 최신 청크 복구.
- **`ErrorBoundary`**: `<App>` 전체를 감싸 어떤 렌더 에러도 검은 화면 대신 "다시 시도" 버튼 화면으로. 청크 에러는 자동 새로고침.
- 다음 배포부터 효과. 네이티브 앱에서만 계속 먹통이면 별도 원인(기기 로그 필요).

---

## 커밋 목록
```
747d7ef 정보 최신글 행 위아래 여백 축소
d60b2c8 정보공유 룸 부제 제거
b61ba1c 정보 최신글: 정보+공모전+기타 통합·제목 축소·상대시간
e4c9e6d 정보공유 개편: 카테고리 정보·공모전·기타(익명) + 홈 정보 최신글 섹션
9b864a3 카테고리 탭·글쓰기 FAB 그라데이션 통일
a1ff393 게시글 상세 그라데이션 통일 (자유/질문 색 분기 제거)
901e38d 새 글 작성 폼 목업 반영 + 톤 통일
0057ddf 검은 화면 먹통 방지: lazy 청크 재시도 + ErrorBoundary
4fc6936 글쓰기 FAB 하단바 위로 (bottom: navH+16)
9292625 페이지네이션 '1~N/전체 n개' 제거
80f9e33 게시글 상세 카테고리별 액센트 (자유=빨강/질문=파랑) ※이후 통일로 대체
10c4b14 상세 리액션·메뉴 디테일 (점세개·무색 따봉/비추·댓글 시간폰트·하트→따봉)
9edc1ae 상세 피드백 (제목 축소·신고차단 삼선메뉴·리액션 순서·문구 제거)
0e9460c 게시글 상세 리디자인 (word.png 목업)
5a026ec TOP3 배너 좌우 정렬 + 검색 드롭다운 화살표
fabaeaa TOP3 헤더 → 박스오피스 배너 이미지
f169199 검색바 개편 (익명게시판 부제 제거·제목/내용 드롭다운·단색 검색아이콘)
```
