# 작업내역 2026-09-26 — 장비대여 앱 → 커뮤니티 앱 재구성

## 개요
장비대여 앱을 **순수 커뮤니티 앱**으로 재구성. **같은 repo · 같은 appId(`com.kbas.rental`) 유지** → 스토어엔 업데이트로 나감(새 심사 X), 유저계정/Firebase 그대로. `src`만 갈아엎음.

## 최종 구조
- **학생 하단바 5탭**: 홈 / 작품제작 / 커뮤니티(가운데 FAB) / 작품상영관 / 더보기
  - 작품제작 = 프로젝트스튜디오 + 촬영도구 (세그먼트 토글, `ProductionScreen.jsx`)
  - 작품상영관 = KBATV 박스오피스 (`Community` `lockRoom="boxoffice"`)
- **관리자 하단바 4탭**: 홈(대시보드) / 학생 / SNS / 더보기

## 커밋 (main, 611e0e5 → e68b01e)
1. **장비/대여/라이선스 제거** — 파일 21개 삭제(admin 7·student 6·공용 7·projectstudio 장비화면 1), App.jsx 라우팅·알림(buildAlerts)·CartProvider·네비(Layout/GroupHub) 정리, projectstudio 장비예약 화면 분리.
2. **학생 홈 재작성** — home-hero.png 원본을 배경(opacity 0.42)+하단 스크림으로 깔고, 빠른메뉴 3버튼(flexbox nowrap flex:1)을 히어로 카드 안에. 대여통계·신뢰도·반납사진·대여규칙/상세모달·라이선스수업 제거, 렌토리·온보딩 문구 커뮤니티톤으로.
   - ⚠️ grid `repeat(3,...)`가 Galaxy S23+ 크롬에서 2+1로 깨져서 **flexbox nowrap로 교체**해야 했음.
3. **작품제작 탭** — 촬영도구 9종을 `FilmTools.jsx`로 추출, `프로젝트↔촬영도구` 토글. Community의 도구 잔재(import·ROOMS "tools"·FILM_TOOL_BOXES·selectedTool·렌더블록 9개) 전부 청소.
4. **관리자 대시보드 재작성** — 대여 위젯 전부 걷어내고 가입승인/비번초기화/문의/커뮤니티활동 위젯으로. GlobalSearch도 장비·대여 검색 제거.
5. **펫 시스템 제거** — `PetGame.jsx` 삭제, 홈 펫카드/오버레이 제거, 친구타일 전체폭 가로 레이아웃.
6. **오늘의 퀴즈 제거** — 펫 화면에서만 풀렸어서 함께 제거. Notices.jsx 퀴즈 등록/대량등록 UI·파서·핸들러 + functions `postDailyQuiz` 스케줄 함수.
7. **Firebase 함수 정리** — `firebase functions:delete postDailyQuiz checkOverdue onRentalCreate onRentalStatusChange --force` 로 실제 삭제. functions/index.js 코드도 동기화(602→377줄). 남은 함수 9개(비번초기화·공지·커뮤니티댓글·가입승인·크루초대/재배정·수동알림·시간표AI·프로젝트AI)는 현역.
8. **Firestore 규칙 정리** (`firestore_rules_복붙용.txt`) — 미사용 컬렉션 13종 제거(equipments·rentalRequests·facilityRequests·licenseSchedules·licenseApplications·facilities·inspections·quizzes·quizPool·scenepatchArticles·reporterApplications·externalRentals·repairs) + users의 pet update 절 제거(남이 pet 필드 통째 교체하던 보안 구멍 차단). Students/Profile의 죽은 rentalRequests 통계도 제거.
9. **QA 수정** — (A) 작품상영관 탭을 박스오피스 전용 고정(`lockRoom`), 뒤로가기 허브 안 뜸. (C) 프로젝트 초대 알림: 학생은 `production` 탭으로(하단바 활성), 관리자는 `projectstudio` 유지.

## 남은 것 (다음 세션)
- [ ] **Firestore 규칙 콘솔 게시** — `firestore_rules_복붙용.txt` 를 Firebase 콘솔 › Firestore › 규칙에 붙여넣고 **게시**해야 실제 반영됨. (아직 안 함)
- [ ] **네이티브 앱 출시** — 지금 웹만 새 앱. 빌드번호↑(pbxproj/build.gradle/appVersion.js) + `npm run build` + `cap sync` + Codemagic(iOS)/플레이스토어(Android).
- [ ] **실기기 수동 QA** — 5탭 흐름·작품제작 토글·작품상영관·알림 딥링크 확인.

## 참고
- 웹 배포는 Vercel 자동(main push). 브라우저로 열면 스토어로 튕김 → 우회 `?web=1`.
- 학생 리디자인 화면은 블루 액센트(#3b82f6/#7c3aed), theme.js C는 아직 모노톤.
- 영상계열만 커뮤니티 접근(hideEverytime)은 의도적 유지.
