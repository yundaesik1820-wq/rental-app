# 작업 내역 — 2026-07-23 커뮤니티 홈 캐러셀·공모전 개편 + 필름도구·슬레이터 픽스

대상 파일: `src/pages/student/Community.jsx`(대부분), `src/components/CinemaSlate.jsx` 외 도구 8종, `src/pages/student/EquipList.jsx`
커밋 범위: `ea60900`(작업 전) → `96c9ada`(현재), 총 11개.
배포: GitHub push → Vercel 자동배포.

---

## 1. 정보 최신글 작성자 숨김 + 필름도구 폰트 통일 (`fc334ce`)
- 커뮤니티 홈 **정보 최신글** 카드에서 작성자 줄(`displayName`) 완전 제거 — 익명 표기도 안 보이게.
- 필름도구 8종(슬레이트·라이브노출·노출계산기·DOF·색온도·화각·태양위치·자료실)의 `FONT_MONO`를 `'Courier New', ui-monospace, monospace` → **`'Noto Sans KR', sans-serif`**로 교체. 영어·숫자도 한글과 같은 고딕체. (`letterSpacing`은 유지, ScripterTool은 원래 모노 미사용)
- ⚠️ 참고: Pretendard는 어디서도 로드 안 함 — 실제 렌더는 `index.html`에서 로드하는 **Noto Sans KR**.

## 2. 필름도구 뒤로가기 간소화 + 슬레이터 픽스 (`cc016b0`)
- 도구 9종 상단 뒤로가기: 노란 박스 "← 도구" → **박스 없는 `‹` 하나** (26px, 골드/스크립터는 C.text). 터치영역용 패딩·`touchAction:manipulation` 유지.
- **슬레이터 메모 터치좌표 버그**: `offsetX/Y`가 회전(transform)된 요소에서 브라우저마다 다름(특히 iOS WebKit은 역회전 안 해줌) → 세로폰(rotate 90deg 래퍼)에서 터치 지점과 다른 곳에 그려짐. **`clientX/Y` 직접 역회전 매핑**으로 수정: `portrait`면 `x = clientY - rect.top, y = rect.right - clientX`.
- **CLAP 동기화**: 누르면 애니메이션만 시작, **빛 번쩍임·진동·소리를 전부 스냅 시점(480ms)으로 모음**. clap.mp3는 진입 시 프리로드(`clapAudioRef`)해서 재생 지연 제거.

## 3. 공모전 새글 템플릿 개편 (`d02972a`)
- 작성 폼(공모전 선택 시): **제목\* / 시작일자\*·종료일자\*(date 피커, min 연동, D-day 미리보기) / 이미지 1장 이상 필수(최대 3장)**. **내용 칸 제거** → 대신 **공모전 링크** 입력(선택).
- 데이터 모델: 종료일자는 협업모집의 **`deadline` 재사용**(목록 D-day 뱃지 공짜), 신규 필드 `contestStart`·`contestUrl`. content는 빈 문자열로 저장.
- 글 상세: D-day 뱃지 + 📅 접수기간 표시, 링크 있으면 **"공모전 홈페이지 바로가기" 풀폭 버튼**(`window.open`, 클래스 채널 링크와 같은 패턴). 기존 글 content는 있으면 그대로 표시.
- 제출 검증·게시버튼 비활성 조건 모두 공모전 전용 분기 추가.

## 4. 공모전 목록·홈 노출 (`e5d644f`, `8c7bd60`, `f62fff9`)
- 목록 카드 카테고리 뱃지 옆 **D-day 미니 뱃지**(주황 `#fb923c`, 마감 시 회색 "마감"). 정렬은 카테고리 뱃지와 동일 규격(fontSize 10, padding 2px 8px)으로 맞춤.
- **정보 최신글에서 공모전 제외** (`p.category !== "공모전"` 필터).
- 홈 정보 최신글~필름도구 사이에 **공모전 정보 캐러셀** 신설: 좌 이미지(100×76, 없으면 🏆) / 우 제목(2줄)·기간·D-day 뱃지. **마감 안 지난 것만, 임박순**. 전체보기 → 정보게시판 공모전 카테고리.

## 5. 캐러셀 무한 루프 (`c05de3e`)
- 되감기(왼쪽 촤르륵) 모션 제거 → **끝 뒤에 첫 장 복제를 붙여 항상 오른쪽으로 진행**, 복제에 완전 도달하면 진짜 첫 장으로 순간이동(티 안 남).
- 공모전 캐러셀(스크롤+snap 방식)과 **장비예약 히어로**(`EquipList.jsx`, transform 방식: 애니메이션 끝난 뒤 transition off 순간이동, 점 인디케이터 `% 길이` 보정) 둘 다 적용.

## 6. 오늘의 추천작 (`ae678ba`, `aa76a73`, `518c76d`)
- 필름도구 아래 **오늘의 추천작** 섹션: 좌 유튜브 썸네일(116×65, ▶ 오버레이) / 우 제목·🎬크레딧·⏱러닝타임. 풀은 작품공유 중 유튜브 링크 유효한 글.
- **매일 00시 교체**: 날짜 문자열 해시 시드 → 서버 없이 모두에게 같은 작품. (오늘의 퀴즈와 유사한 클라 시드 방식)
- `aa76a73`에서 **3개 캐러셀**로 확장: 시드 LCG 셔플로 3개 선정. 캐러셀 로직은 **`AutoCarousel` 공용 컴포넌트로 리팩터링**(4초 자동·손 스와이프·무한 루프·터치 중 정지·점 인디케이터 accent色) — 공모전(`ContestCarousel`)·추천작(`WorkCarousel`) 모두 이걸 사용.
- 전체보기 → **KBATV 박스오피스** 룸 (`518c76d`).

## 7. 필름 클래스 캐러셀 (`96c9ada`)
- 추천작 아래 **필름 클래스** 섹션(`ClassCarousel`, 인디고 `#6366f1`): 좌 첫 강의 유튜브 썸네일(없으면 🎓) / 우 강좌명·분야/부제·n강.
- 매일 3개 시드 셔플(추천작 시드에 XOR 섞어 다른 조합), 전체보기 → 필름 클래스 룸. 규칙은 추천작과 동일.

---

## 홈(커뮤니티 탭) 최종 섹션 순서
프로젝트 배너 → 에타/질문 최신글(2열) → 정보 최신글 → **공모전 정보(캐러셀)** → 필름도구 → **오늘의 추천작(캐러셀)** → **필름 클래스(캐러셀)**

## 남은 확인
- 슬레이터 메모: 아이폰 세로로 실기기 확인 필요(회전 매핑이라 데스크톱 검증 불가).
- 공모전: 실제 글 작성해서 필수 검증·상세 링크 버튼·캐러셀 노출 확인.
- 캐러셀 무한 루프: iOS 모멘텀 스크롤에서 복제→첫 장 순간이동 자연스러운지 확인.
