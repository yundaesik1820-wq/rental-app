# 작업 내역 — 2026-07-19 (푸시 알림 개편 · 알림 배너 · 1.0.11 릴리스)

> 학생 UI 블루 리디자인/하단바 개편은 앞선 세션 참고. 이 문서는 그 이후 **알림 시스템 개편 + 릴리스** 작업.

## 1. 교사 서명대기 단계 임시 스킵 → 원복
- 테스트 속도 때문에 `Rental.jsx`의 `approve`를 **승인대기 → 승인됨 직행**으로 잠시 변경(교사서명대기 스킵).
- 점검 종료 후 **원복** (승인대기 → 교사서명대기 → 교사 2차 서명 → 승인됨).

## 2. 예약내역 UI (`History.jsx`)
- 우측 화살표 제거 → **박스 하단 중앙 ⌄ 화살표**로 이동. 펼치면 180° 회전(스프링), 상세는 `historyExpand` 페이드+슬라이드.
- 카드에 **누름 바운스**(`card-press`, `index.html`에 클래스+키프레임 추가).
- 신청서 뷰 **서명 박스: 흰 배경 + 중앙정렬** (검은 서명이 검은 배경에 안 보이던 문제 해결).
- 대여중 반납사진 안내 문구 변경 → "촬영현장에서 장비를 사용했던 모습을 찍고 올려주세요!" + 빨간 글씨 "(3장을 업로드해주셔야하며 미업로드시 반납처리가 불가합니다.)"

## 3. 예약 캘린더 통계 확인
- 학생 뷰 "이번 달 예약/반납완료" 건수 = **로그인 학생 본인 기준**(`Calendar.jsx` `isMineReq`) 확인만. 코드 변경 없음.
- ⚠️ `isMineReq` 2번 조건 `r.phone === userEmail`은 전화≠이메일 비교라 사실상 매칭 안 됨(복붙 흔적).

## 4. 푸시 알림 전면 개편 (`functions/index.js`) — Cloud Functions
- **상태별 문구 리라이팅** (승인됨🎉 / 거절됨😢 / 반납완료 / 보류 / 연체🚨).
- **대여중 알림 신규** — "멋진 작품 만드시길 바래요!" + 촬영 응원 + 사진업로드 리마인드.
- **반납 1시간 전 알림 신규** — 기존 `checkOverdue`(30분마다)에 얹음. `preReturnAlerted` 플래그로 1회만 발송.
- **연체 중복 알림 정리** — 연체 푸시를 `onRentalStatusChange` 단독 발송으로 통합(`checkOverdue` 직접 발송 제거, `overdueAlerted:false` 초기화는 유지).
- **신청 접수 알림 신규 (`onRentalCreate`)** — 학생이 신청 제출 시 "신청이 완료됐어요!🔥".
- **배포**: `firebase deploy --only functions:onRentalStatusChange,checkOverdue,onRentalCreate` (프로젝트 `kbas-equipment-rental`).
- **잔재 함수 정리**: 시설 대여 기능 제거 시 소스에서만 지워진 `onFacilityStatusChange`(us-central1)가 프로덕션에 남아 전체 배포를 막고 있었음 → **삭제**. 이제 배포 함수 10개 = 소스 일치, 전체 배포 정상.

## 5. 푸시 알림 유도 배너 (하단바 위, 학생 전용)
- `Layout.jsx`: 학생 + 알림 미허용(`granted` 아님) 시 하단바 바로 위에 배너 노출. 하단바 높이 실측(`navRef`)해서 위치 고정, FAB 걸침 여백 22px.
- `useFCM.js`: **`enableNotifications(userId)`**(온디맨드 권한요청+토큰등록) / **`getNotifPermissionState()`** 추가. 켜지면 배너 자동 숨김. 거부 상태면 OS 설정 안내.
- 배너 이미지 = **`public/notif-banner.png`** (바탕화면 `dddd.png` → 투명 여백 크롭 1903×483 → 리사이즈 **1140×289**). 이미지가 박스 전체(배경+마스코트), 그 위에 텍스트+버튼만 오버레이(우측 정렬, 박스 중앙 세로정렬).
- 문구: **"대여 상태가 바뀌면 바로 알려드려요! / 승인부터 반납까지, 중요한 안내를 놓치지 마세요."** (8.5 / 7.5px).

## 6. 1.0.11 릴리스 준비
- 1.0.10이 스토어 승인/출시됨(트레인 닫힘) → **마케팅 1.0.11로 3곳 동기화**: `appVersion.js` / iOS `pbxproj`(MARKETING 1.0.11) / Android `build.gradle`(versionName 1.0.11).
- 빌드번호: iOS `CURRENT_PROJECT_VERSION` 13(Codemagic이 자동 재조정), Android `versionCode` 13.
- 웹 빌드 로컬 검증 통과 → **Android `npx cap sync android` 완료**(배너 이미지·푸시 코드 반영 확인).
- 빌드 방법: **iOS = Codemagic `KBAS iOS (심사·출시)` 워크플로우 수동 실행**(자동 npm build→cap sync→빌드번호+1→TestFlight). **Android = 로컬 `gradlew.bat bundleRelease` → Play Console**.

## 7. 출시노트 작성 (1.0.11)
- 학생 화면 리디자인 / 하단바 장비예약 버튼 / 예약내역·캘린더 개선 / 알림 강화(반납 1시간 전 등) / 버그 수정.

## 확인/후속
- [ ] **iOS Codemagic 빌드 실행** + TestFlight/App Store 심사 제출.
- [ ] **Android AAB 빌드**(`gradlew.bat bundleRelease`) + Play Console 업로드.
- [ ] 실기기(네이티브 앱): 알림 권한 팝업 + 배너 "알림 설정" 동작, 상태 변경별 푸시 문구·타이밍, 반납 1시간 전 알림.
- [ ] 필요 시 강제 업데이트 게이트(Firebase `config/appVersion` 최소버전) 상향 — 지금은 미변경.
- 배포: 코드/이미지/버전 모두 push 완료(Vercel 자동 배포). Functions 배포 완료.
