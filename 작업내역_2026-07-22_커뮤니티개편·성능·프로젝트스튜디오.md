# 작업내역 2026-07-22 — 커뮤니티 대개편 · 성능 최적화 · 프로젝트 스튜디오

버전: **1.0.12 / versionCode 15** (Android Play 재업로드 대기)
커밋 범위: `8f00d53`(전날) 이후 → `26c0543` (총 14개 커밋, 전부 origin/main 푸시 완료)

---

## 0. Play Store 업로드 이슈 진단 (근본 원인)
- **증상**: 앱 버전은 1.0.12인데 장비목록 리디자인·Project Studio 등 최신 화면이 폰에 안 보임.
- **원인**: Android 빌드 시 `npm run build` + `npx cap sync android`를 빼먹고 Android Studio에서 바로 aab를 만들어서, **versionCode/Name만 최신이고 안에 든 웹 번들은 옛날 dist**인 aab가 Play에 올라갔던 것. (iOS는 codemagic이 자동으로 build+sync 해서 안 겪음)
- **조치**: build+sync 실행해서 android에 최신 번들 주입, `versionCode 14→15`로 올림(14 이미 사용됨).
- **재발 방지**: "플레이스토어 업로드하자" 하면 빌드번호↑ + build + sync 3개를 자동 실행하도록 메모리에 기록.

---

## 1. 성능 최적화 (`b257fdc`)
- **이미지 lazy loading 14곳**: 커뮤니티 첨부사진·장비 카드·반납사진 등 리스트 이미지에 `loading="lazy" decoding="async"` → 화면에 보이는 사진만 로드. (라이트박스·프린트·로고는 제외)
- **번들 코드 분할**: App.jsx 페이지들을 `React.lazy` + `<Suspense>`로 → **초기 번들 2.7MB → 832KB (약 70%↓)**. 학생은 관리자 전용 무거운 코드(recharts 374KB·jsqr·xlsx 419KB)를 초기에 안 받음.

---

## 2. 커뮤니티 상단바 수정 (`b257fdc`)
- **앱 상단바 위치**: `Community.jsx` `SAFE_FLOOR` 48→24 → 다른 화면 루트(`max(env,24)`)와 동일 기준. 앱에서 커뮤니티 상단바가 너무 내려오던 것 정렬.
- **좌상단 1px 깜빡임**: safe-area 측정용 probe를 `left:0`(좌상단) → `left:-9999px`(화면 밖)으로. 높이만 재는 요소라 위치 무관.

---

## 3. 커뮤니티 홈 대개편 (룸 허브 → 피드형)
- **박스룸 카드 그리드 제거 → 최근 게시글 피드** (`43ba639`)
- **2열 카드로 재구성** (`f39176c`, `2e161a6`, `6d94db2`, `11748ba`)
  - 왼쪽 **에타 최신글**(자유, 핑크), 오른쪽 **질문 최신글**(질문, 파랑)
  - tmtit2 목업 참고: 다크 카드 + 그라데이션 테두리 + 파란 번호 + 전체보기 링크
  - 제목은 박스 밖, 이모지 제거, **min-width:0 체인으로 폰 폭 무관 한 줄 ellipsis 반응형**
  - 전체보기 → `community` 룸(자유/질문 탭 자동 선택)
- **헤더-배너 간격 축소** marginTop 20→4 (`e2f6256`)
- **게시글 목록 = 카드형 리스트** (com.png 참고, `03a32cb`)
  - 카테고리별 색상 뱃지(자유 핑크/질문 파랑/강의 보라/정보 초록/취업 청록/공모전 주황)
  - 제목 볼드 + 본문 2줄 + 우측 썸네일(images[0], 📷N) + 하단 메타(시간·조회·좋아요·댓글)
  - **프로필 원형사진·아이디 제거**
- **룸 화면 시네마 헤더 → 기본 헤더** (`a31ad65`)
  - `← ROOMS`·`● REC`·시네마 룸제목·그라데이션 헤더 제거 → 홈과 동일한 기본 헤더 + 왼쪽 심플 뒤로가기(ChevronLeft)

## 4. 커뮤니티 기능 정리
- **씬스패치·중고장터 완전 삭제** (`e285c0b`): ScenePatch.jsx(41KB) 삭제, scenepatch/marketplace 룸·카테고리·App.jsx 기사 알림 제거. SNS관리 탭(별개 마케팅 도구)은 유지. **817줄 삭제.**
  - ⚠️ Firestore `scenepatchArticles` 데이터·`reporterStatus` 필드는 코드로 못 지움 → 콘솔에서 별도.
- **새내기 카테고리 제거** (`a7cefa9`): CATEGORIES/ANON_CATS/룸탭/색맵에서 제거. 내부 신입생 검증 로직은 실행 경로 없어져 dead code로 잔존(무해).

---

## 5. 프로젝트 스튜디오 (`de43bab`, `79151b1`, `26c0543`)
- **진입 판단**: 커뮤니티 배너 진입 시 진행 중 프로젝트가 있으면 목록, 없으면 새 프로젝트 화면. (`view=null` 로딩 대기 후 `active.length`로 분기)
- **목록 상단 뒤로가기(←)**: `onExit` prop → 커뮤니티 복귀.
- **대시보드 진행 단계 스텝**: 아이디어-프리프로덕션-프로덕션-포스트프로덕션-완료. 점+연결선+라벨, 반응형 한 줄, 완료 단계 보라 강조. 기획(planning)은 아이디어로 흡수.
- **진행단계 5단계로 축소**: `PROJECT_STAGES`에서 기획(planning) 제거 → 새 프로젝트/수정 버튼 둘 다 5단계.
- **진행단계 버튼 한 줄 반응형**: `flexWrap` 제거 → `flex:1` 균등분할, 폰트 9px·자간 축소로 어떤 폰에서도 한 줄.

---

## 남은 할 일 / 다음 세션
- [ ] **Android Play 업로드**: versionCode 15로 Android Studio 서명 aab 빌드 → Play Console 업로드. (build+sync는 최신 반영됨, 바로 빌드 가능)
- [ ] **iOS 반영**: Codemagic `ios-release` 워크플로우 실행 필요 (오늘 변경분 아직 앱 미반영).
- [ ] (선택) Firestore `scenepatchArticles`·`reporterStatus` 콘솔에서 정리.
- [ ] (선택) 커뮤니티/App의 `deepArticleId`·`articleId` 딥링크 데드코드 정리.
- [ ] 커뮤니티 나머지 룸(지식/도구/박스오피스/클래스/작품공유/협업모집/스탭프로필) 정리 방향 미정 — "나머지는 나중에".
