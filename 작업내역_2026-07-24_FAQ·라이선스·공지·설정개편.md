# 작업 내역 — 2026-07-24 문의 FAQ 신설 · 라이선스/공지 리디자인 · 설정 확장 · 1.0.13

대상 파일: `src/App.jsx`, `src/pages/student/Inquiry.jsx`·`License.jsx`·`Profile.jsx`, `src/pages/admin/Inquiry.jsx`·`Notices.jsx`·`Settings.jsx`, `src/data/inquiryCategories.js`·`faqSeed.js`(신규), `src/appVersion.js`, `src/components/UpdateGate.jsx`, `firestore_rules_복붙용.txt`, `CLAUDE.md`, 버전 3곳
커밋 범위: `ee0d49b`(작업 전) → `3c22323`(현재), 총 9개.
배포: GitHub push → Vercel 자동배포.

---

## 0. 시간표 공개 토글 정렬 (`b823076`)

동그라미보다 네모(트랙)가 커 보이던 문제. **첫 수정은 효과가 없었고**, 원인은 따로 있었음.

- **원인**: `Layout.jsx:293` 모바일 미디어쿼리의 전역 `button { min-height: 36px }`. `min-height`는 인라인 `height`를 **항상 이김** → 폭 48만 먹고 높이는 36으로 강제됐음. 스샷 픽셀 실측(트랙 54px / 원 33px ÷ 배율 1.5 = 36 / 22)으로 확인.
- **해결**: 전역 규칙은 다른 버튼 터치영역 때문에 두고, 이 토글에만 **인라인 `minHeight: 22`** 추가. 트랙 48×22 / 노브 22×22.
- ⚠️ **CLAUDE.md에 이 함정을 한 줄 기록해둠** (`aca99d1`). 작은 버튼(토글·칩·아이콘 버튼)이 부풀면 `height`만 고치지 말고 `minHeight`를 같이 줄 것.

## 1. 문의하기 FAQ 신설 (`234cb92`)

기존엔 최상단이 "+ 문의 작성"이라 학생이 FAQ를 볼 기회 없이 바로 문의를 썼음 → **순서를 뒤집어 문의량을 줄이는 구조로.**

```
🔍 검색 → 카테고리 칩 → FAQ 아코디언
→ "원하는 답을 못 찾으셨나요?" + 문의 작성
→ 문의 폼(제목·내용 입력 시 💡 관련 FAQ 자동 추천) → 내 문의 내역
```

- **`src/data/inquiryCategories.js`(신규)** — 문의·FAQ **공용** 카테고리 5개(대여 / 반납·연체 / 라이선스 / 계정 / 기타). 옛 표기(`라이센스 문의`) 정규화 + 목록에 없는 구 카테고리(`일반 문의`)도 아이콘·필터칩에 살아남게 `catFilterList`로 폴백. **id는 Firestore 저장값이라 바꾸면 기존 문서와 어긋남 — 추가만 할 것.**
- **`src/data/faqSeed.js`(신규)** — 기본 FAQ 13개. CLAUDE.md에 있는 실제 정책만 사용(3일 전 신청·긴급 예외, 금 17:30~월 09:00, 예약 상태 흐름, 반납사진 위치, 라이선스 잠금, 가입 승인 등).
- **관리자 문의 관리에 `[💬 문의 답변] / [❓ FAQ 관리]` 토글 추가.** FAQ 추가·수정·삭제 + ▲▼ 순서 이동(`order` 스왑). FAQ 0개면 **"기본 FAQ 13개 불러오기"** 버튼.
- **관련 FAQ 자동 추천**: 제목+내용을 2글자 이상 토큰으로 쪼개 FAQ 질문·답변과 겹치는 개수로 점수 → 상위 3개.
- **규칙**: `faqs` 읽기=로그인 / 쓰기=관리자. **콘솔 게시 완료.**
- ⚠️ 시드 답변 중 **연체 처리**·**라이선스 등급별 차이**는 실제 정책을 몰라 두루뭉술하게 써둠 → 등록 후 직접 수정 필요.

## 2. 라이선스 화면 리디자인 (`d86b6a8`)

기존이 `theme.js`의 `C` 모노톤을 써서 다크에서 값이 어긋나 있었음:
- 히어로가 `linear-gradient(135deg, C.navy, #2D4A9B)` — 다크에서 `C.navy`는 **흰색**이라 흰색→남색 그라데이션이 떴음
- 안내 박스가 `C.yellowLight`(#2E2200) 배경에 `#92400E` **갈색 글자** (라이트 테마용 조합 잔재)

**더보기 메뉴 라이선스 타일(`App.jsx:339`)이 이미 쓰는 값을 그대로 확장**: 액센트 `#7c3aed`, 배지 테두리 `rgba(124,58,237,.5)`/글자 `#c4b5fd`, 진행바·버튼 `linear-gradient(90deg,#3b82f6,#7c3aed)`. 상태 배지는 `History.jsx`의 `PAL` 값. 단계 배지는 **1 블루 / 2 바이올렛 / 3 골드**.

- 히어로: 글로우 + 그라데이션 숫자 + 3분할 세그먼트 진행바
- 일정 카드: 날짜·시간 아이콘 칩, **정원 진행바 신설**, 그라데이션 신청 버튼
- **빈 상태 2종 신설** — 일정 없음(공지사항 이동 버튼) / 최고 등급(트로피). 기존엔 `myLevel < 3` 조건 때문에 **Lv.3이면 섹션이 그냥 빈칸**이었음
- `App.jsx`에 `onOpenNotices` prop 연결 (더보기 경로·탭 경로 both)
- **`CREW LEVEL` → `LICENSE LEVEL`** (라이선스 화면 + 더보기 타일 둘 다. 같은 배지라 한쪽만 바꾸면 어긋남). 360px 폰에서 레이아웃 안 밀리는 것 확인 — 아래 진행바(86px) 줄이 이미 우측 컬럼 폭을 결정하고 있어서 배지가 21px 넓어져도 영향 없음
- 신청 로직(`canApply`/`apply`)과 알림 딥링크는 그대로, 하이라이트 색만 변경

## 3. 공지사항 학생 뷰 리디자인 (`068db4f`)

`Notices.jsx`는 **관리자·학생 공유 컴포넌트**(`isAdmin` prop) → CLAUDE.md의 `Calendar.jsx` 전례대로 **`!isAdmin`일 때만** 리스타일. 관리자 목록·공지 작성 모달·푸시 알림 발송·퀴즈 등록은 **안 건드림**.
상세 모달은 마크업이 같아서 색만 `T` 토큰으로 분기했고, **관리자 값은 기존 `C.*`를 1:1 매핑**해 렌더 결과 동일.

- 톤: 더보기 공지사항 타일 tint **`#38bdf8`(sky)** 를 액센트로. 카테고리는 `NOTICE_CAT`의 의미(공지/신규장비/휴무) 유지하되 `PAL` 계열 값으로 교체
- 댓글 수가 `C.blue`(다크에선 **흰색**)라 제목과 구분이 안 되던 것도 sky로
- **카테고리 필터 칩 신설**(전체/공지/신규장비/휴무) — 문의 FAQ 화면과 같은 칩 패턴
- 카드: 고정 공지 좌측 그라데이션 바 + sky 테두리, 고정·PDF 배지, 메타 한 줄 정리
- **빈 상태 신설** — 필터 결과 없을 때 문구 분기(`'휴무' 공지가 없어요`)
- 상세: 역할 배지 색 정리(**기존엔 관리자·교사가 둘 다 흰색**이었음), 닫기 버튼 sky 아웃라인
- **댓글 입력칸 정렬 수정**: 안내문("Enter로 등록...")이 입력칸과 **같은 wrapper 안**에 있어서 `alignItems:flex-end`가 안내문 바닥에 맞춰지며 버튼만 툭 떨어져 보였음 → 안내문·wrapper 제거, 아바타·입력칸·등록버튼 **중심선 정렬**(입력칸 56 / 버튼 40 / 아바타 34). **Enter 등록 단축키는 그대로 동작**(핸들러 유지, 문구만 제거)

## 4. 설정 화면 확장 (`2df59a6`)

푸시 알림 + 앱 정보 둘뿐이던 걸 **알림 / 개인정보 / 앱 관리 / 정보** 4섹션으로.

- **알림 진단**(신규) — `users/{uid}`를 새로 읽어 `webTokens`/`nativeTokens` 개수 + **이 기기 토큰 등록 여부** 표시. 권한이 이미 `granted`일 때만 현재 토큰을 조회해 **진단 때문에 권한 팝업이 뜨지 않게** 함. 미등록일 때만 `다시 등록` 노출(`enableNotifications` 재호출, `arrayUnion`이라 중복 없음)
- **앱 새로고침**(신규) — SW 등록 해제 + `caches` 전체 삭제 후 reload. 검은 화면(청크 로드 실패) 때 **학생이 직접 복구**할 수단
- **업데이트 확인**(신규) — `config/appVersion` 비교, 새 버전이면 `UpdateGate`와 같은 스토어 딥링크. 웹은 "항상 최신" 안내
- **이동**: 시간표 공개 토글 + 개인정보처리방침을 `Profile.jsx` → 설정으로. 내 정보엔 프로필·계정 정보와 로그아웃/탈퇴만 남음

## 5. 버전 확인 플랫폼별 분리 (`8f014ec`)

`APP_VERSION`은 같은 웹 번들이라 두 플랫폼이 공유하는데, 스토어 버전은 **심사 시차로 어긋남**. 그런데 `config/appVersion`엔 `minVersion` 하나뿐이라 한쪽만 강제하거나 한쪽만 최신이라고 안내할 방법이 없었음.

- `appVersion.js`에 **`pickPlatformVersion(cfg, base, platform)`** 추가 → `minVersionIos`/`minVersionAndroid`, `latestVersionIos`/`latestVersionAndroid`를 우선 읽고 **없으면 기존 공용 필드로 폴백**(= 지금 문서 그대로면 동작 동일)
- `UpdateGate`(강제 업데이트 게이트) + 설정 업데이트 확인 양쪽에 적용
- 폴백 **11개 케이스 검증 완료** — 공용 필드만 있을 때 기존 동작 유지 / 한쪽 플랫폼만 강제해도 반대편 안 막힘 / `enabled:false` 우선 / 형식 오류는 판정 제외

## 6. 관리자 앱 버전 관리 카드 (`efc62da`)

`config/appVersion`이 **콘솔에서만 만질 수 있는 유일한 운영 데이터**라 릴리스마다 브라우저를 열어야 했음 → 관리자 › 더보기 › 설정 맨 아래에 편집 카드 추가.

- iOS/Android **최신 버전** + **강제 업데이트 기준선** + on/off 토글 + 잠금 안내 문구
- **안전장치**: 입력값이 현재 배포본(`APP_VERSION`)을 잠그면 입력칸이 빨갛게 + 경고 배너 + 저장 버튼이 `⚠️ 잠금을 적용하고 저장`(빨강)으로 전환. 버전 형식(숫자·점) 검증으로 `v1.0.12` 같은 값 차단. **11개 케이스 검증 완료**
- `merge:true` 저장 + 빈 칸은 payload 제외 → `iosUrl` 등 기존 필드 보존
- 초기값을 기존 공용 `minVersion`/`latestVersion`으로 채워서 **한 번 저장하면 플랫폼별 필드로 자연 분리**
- **규칙**: `config`가 `write: if false`라 관리자도 못 쓰던 것을 **`isSuperAdmin()`** 으로 개방. `isAdmin()`은 `role=='admin'`만 봐서 **교수·교사(제한 권한)도 통과**하는데 앱을 잠글 수 있는 설정이라 **super/assistant로 좁힘**. 화면도 `isSuper`일 때만 노출

## 7. 1.0.13 버전업 (`3c22323`)

1.0.12(`8f00d53`, 7/21) 이후 기능이 많이 들어가 마케팅 버전 상향. **3곳 동시**:

| 파일 | 값 |
|---|---|
| `src/appVersion.js` | `APP_VERSION = "1.0.13"` |
| `android/app/build.gradle` | versionName `1.0.13` / versionCode **16** |
| `ios/.../project.pbxproj` | MARKETING_VERSION `1.0.13` / CURRENT_PROJECT_VERSION **15** |

iOS 빌드번호도 14→15로 함께 올림(14는 1.0.12에서 이미 쓴 번호). **Android 16 / iOS 15로 번호가 다른 건 정상** — 스토어별 독립.
`npm run build` + `npx cap sync android` 완료 → **AAB 빌드 준비된 상태.**

---

## 다음에 할 일 (우선순위)

1. **[필수] Firestore 규칙 게시** — `config` 쓰기 권한(`isSuperAdmin()`). **안 하면 관리자 버전 관리 카드 저장이 권한 오류로 막힘.** `firestore_rules_복붙용.txt` 붙여넣기. ⚠️ `match /config` 블록만 바꾸고 **`isSuperAdmin()` 함수를 빼먹으면 규칙 자체가 컴파일 안 됨**
2. **Android Studio에서 AAB 생성 → Play Console 업로드** (빌드 준비 완료 상태)
3. **Play 출시 완료 후** 관리자 › 설정 › 앱 버전 관리에서 `latestVersionAndroid = 1.0.13`. **미리 넣지 말 것** — 아직 못 받는 버전을 "새 버전 나왔어요"라고 안내하게 됨
4. **iOS는 Codemagic → TestFlight → 심사 통과 후** `latestVersionIos`만 따로 갱신 (플랫폼별로 나눠둔 이유가 이것)
5. **FAQ 기본 13개 등록** — 관리자 › 문의 관리 › FAQ 관리 › "기본 FAQ 불러오기" → **연체 처리·라이선스 등급별 차이 답변 직접 수정**
6. **스토어 실제 승인 버전 확인 후 CLAUDE.md 출시 상태 섹션 갱신** — 지금 적힌 "1.0.9 (빌드 11) 심사 중"은 오래된 정보

## 실기기 확인 목록 (갤럭시23 / 아이폰16)

- 문의하기: FAQ 검색·카테고리 칩, 문의 폼에서 제목 입력 시 💡 관련 FAQ 추천
- 라이선스: 일정 있을 때/없을 때/최고 등급 3가지 상태, 공지사항 이동 버튼
- 공지사항: 카테고리 필터, 댓글 입력칸·등록버튼 정렬, **Enter로 등록되는지**
- 설정: 알림 진단(권한 팝업 안 뜨는지), 앱 새로고침 후 정상 복구, 업데이트 확인
- **관리자 공지·문의 화면이 예전 그대로인지** ← 공유 컴포넌트라 이게 제일 중요
