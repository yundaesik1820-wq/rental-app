# 작업 내역 — 2026-07-23 더보기 다듬기·내 정보 개편·프로필 사진 시스템

대상 파일: `src/App.jsx`, `src/pages/student/Profile.jsx`·`Home.jsx`·`FriendManager.jsx`, `src/components/UI.jsx`, `src/hooks/useAuth.jsx`, `storage_rules_복붙용.txt`(신규)
커밋 범위: `0b9de33`(작업 전) → `ee0d49b`(현재), 총 4개.
배포: GitHub push → Vercel 자동배포. **Firebase 콘솔 Storage 규칙 게시 완료**(필수 선행이었음).

---

## 1. 더보기 히어로 다듬기 (`dafab9b`)
- 아바타 옆 연필 버튼 제거 (프로필 진입은 "내 정보" 메뉴 카드로 충분).
- **KBAS MEMBER 배지 → 🎬 `LV{n} 라이선스` 배지** 교체 (Clapperboard 아이콘, 기존 `licNum` 재사용, 퍼플 톤 유지).

## 2. 내 정보 페이지 개편 (`9686922`, `0d9efa5`)
- **더보기와 동일한 블루·퍼플 다크 톤**으로 전면 리스타일 — 로컬 팔레트 `P`(카드 `#121218`, 보더 `rgba(255,255,255,0.07)` 등). 상단 프로필 카드 → 퍼플 그라데이션 히어로 + 글로우 링 아바타. 모달 3종(비번/전화/탈퇴)은 공용 `Modal`이라 그대로.
- **정리**(`0d9efa5`): 장비 사용 라이선스 카드 삭제, 계정 정보에서 이메일 줄 삭제, 화면 테마 카드 삭제(+ `setTheme`/`themeMode` 등 관련 코드 정리), **로그아웃·회원 탈퇴 반반 2열**(둘 다 `flex:1` + 동일 padding/border/boxSizing).
- **시간표 공개 토글 수정**: 전역 button 스타일에 눌려 찌그러지던 것 → `padding:0`+`minWidth`+`boxSizing` 고정, 48×28 노브 22px. (연필 타원화와 같은 원인 — **원형/고정폭 버튼은 항상 이 3종 세트 필수**)

## 3. 프로필 사진 시스템 신설 (`9686922`)
- **업로드 플로우**: 내 정보 아바타의 **원형 연필 버튼**(26px, padding:0 고정) → 파일 선택 → **`PhotoCropModal`**: 240px 원 안에 실제 프로필처럼 미리보기, **드래그로 위치 + 슬라이더 1~3배 줌**(가장자리 클램프) → **확인을 눌러야만** 보이는 영역 그대로 512px JPEG 크롭 → Storage `profile-photos/{uid}.jpg` 업로드 → `users.photoURL` 저장.
- `useAuth`에 **`setProfile` 노출** → 업로드 직후 더보기 히어로 등 즉시 갱신(재시작 불필요).
- `Avatar`(UI.jsx)에 **`src` prop** — 있으면 사진 원형, 없으면 기존 이니셜. `maxWidth:none` 인라인(전역 max-width 함정 회피, 크롭 모달 이미지도 동일).
- **Storage 규칙**: `profile-photos/{fileName}`은 **본인 `uid.jpg`만** 쓰기 허용(이미지, 5MB 제한). ⚠️ 콘솔 기존 규칙에 코드에 없는 레거시 경로(`facilities`/`signatures`/`profiles`/`scenepatch`)가 있었음 — **scenepatch는 공개 읽기라 빼먹으면 깨짐**. 병합본을 `storage_rules_복붙용.txt`로 레포에 보존(= 콘솔 게시본).

## 4. 프로필 사진 학생 화면 확산 (`ee0d49b`)
- App.jsx `AppContent`가 알림용으로 **이미 구독 중인 `allUsers`**로 `photoMap`(uid→photoURL) 생성 → prop 전파. **추가 쿼리/구독/규칙 변경 0**. onSnapshot이라 실시간 갱신.
- 적용: **친구관리**(받은 요청 + 친구 목록, 34px), **홈 친구 시간표**(친구 행 28px), **공지 댓글**(기존 Avatar에 src 연결).
- **의도적 제외**: 커뮤니티(익명 게시판 — 사진 붙이면 익명성 깨짐), 펫 랭킹(펫 이미지 중심), 캘린더(uid 없음). 새 화면에 사람 아바타 넣을 땐 photoMap prop 내려받아 `src` 연결하면 됨.

---

## 남은 확인
- 실기기(갤럭시23/아이폰16): 사진 업로드는 웹에서 성공 확인 — **네이티브 WebView에서 크롭 드래그/줌 감도** 확인 필요(`touchAction:none` 처리함).
- 친구관리·친구 시간표·공지 댓글에서 상대방 사진 표시 확인 (사진 없는 유저는 이니셜 폴백).
- `firestore_rules_복붙용.txt`는 콘솔과 이미 동일 확인 (photoURL은 권한 필드 아님 — Firestore 수정 불필요).
